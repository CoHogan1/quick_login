<nav class="navPartial">

    <a href="/home/index" class="">View All Database Data</a>
    <a href="/home/index/new">Add To the Database</a>

    <% if (currentUser) { %>

        <div>Current User:
             <%=currentUser.username.charAt(0).toUpperCase() + currentUser.username.slice(1)%></div>
        <div>

        <form class="remove" action="/sessions?_method=DELETE" method="POST">
            <input class="end" type="submit" value="logout" ></input>
        </form>

        </div>

    <% } else { %>

        <a class="navUsers" href="/sessions/new">Log In</a>
        <a class="navUsers" href="/users/new">Sign Up</a>

    <% } %>
    <div></div>
</nav>
